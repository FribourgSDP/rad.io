<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Radio" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="colorOnSurface">@color/dark_grey</item>
        <item name="errorTextColor">@color/red</item>
        <item name="android:fontFamily">@font/jost</item>
    </style>

    <!-- Main application theme. -->
    <style name="Theme.Radio.Main" />

    <!-- Theme for the splash screen -->
    <style name="Theme.Radio.Splash">
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorPrimary</item>
    </style>
</resources>